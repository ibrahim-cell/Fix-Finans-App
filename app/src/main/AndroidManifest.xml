<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@drawable/fix_finans_logo"
        android:label="Fix Finans"
        android:roundIcon="@drawable/fix_finans_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.FixFinans">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Fix Finans">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="android.support.customtabs.trusted.DEFAULT_URL"
            android:value="https://ibrahim-cell.github.io/Fix-Finans-App/" />

        <meta-data
            android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
            android:resource="@color/fix_finans_bg" />
        <meta-data
            android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
            android:resource="@color/fix_finans_bg" />
    </application>
</manifest>
